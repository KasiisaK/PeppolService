This project was done as a part of a course. For a more detailed explanation of the implementation refer to the Arrowhead_Report. 

How to launch the application:
* Clone the repository.

Run the frontend in dev mode:

* download the dependencies from the requirements.txt file (do pip install -r requirements.txt, note need pip beforehand).
* in the PeppolFrontend folder do: npm run dev, to launched the application in dev mode.

Run the backend: